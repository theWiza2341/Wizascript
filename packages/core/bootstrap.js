// Waits for window.underscript to be ready, then registers the single
// suite-wide Plugin() instance and hands it to each feature package.
// Replaces the separate polling/retry logic each script currently
// reimplements on its own.
