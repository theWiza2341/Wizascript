# core

Shared utilities used by every feature package: UnderScript bootstrap/plugin
registration, debug logging, settings helpers, page-window (unsafeWindow)
resolution, and card/event correlation helpers.

Not a standalone userscript — consumed by the other packages/* modules.
