{
  "name": "wizascript",
  "version": "1.4.1",
  "private": true,
  "description": "All-in-one UnderScript plugin suite for Undercards.",
  "license": "UNLICENSED",
  "scripts": {
    "build": "node build.js",
    "build:dev": "node build.js --dev"
  },
  "devDependencies": {
    "esbuild": "^0.21.0"
  }
}
