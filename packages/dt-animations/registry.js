// register(cardId, { play, reset, forceStop, isActive }) - replaces
// the one-off window.__titanEyeEffect global pattern so the loader can
// support many DTs without a hardcoded per-card dispatch chain.
