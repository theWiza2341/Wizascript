// Shared toggleable, prefixed console logging. Needs to support both
// category flags (e.g. "cow", "draw") and severity levels (log/warn/
// error), since different scripts have wanted both independently.
